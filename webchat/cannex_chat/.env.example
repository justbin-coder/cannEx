# CannEx Webchat — 环境变量模板
# 复制为 .env 并按实际环境填写，.env 不要提交到 git

# ── LLM 配置（必填其一）─────────────────────────────────────────────
# 服务端兜底 API key（可选；用户通过 BYOK UI 自带 key 时此项可留空）
# ANTHROPIC_API_KEY=

# Anthropic API base URL（使用代理时填写，默认走 Anthropic 官方）
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# 推理模型，默认 claude-sonnet-4-6
# CANNEX_MODEL=claude-sonnet-4-6

# ── 认证（团队部署时填写）───────────────────────────────────────────
# 本地开发跳过登录认证（设为 1）
# CANNEX_DISABLE_AUTH=1

# ── 路径（通常无需设置）──────────────────────────────────────────────
# 项目根目录。app.py / paths.py 会从文件位置自动推导，仅在从项目外启动时才需要。
# CANNEX_ROOT=

# 日志目录，默认 <项目根>/logs
# CANNEX_LOGS_DIR=

# SKILL.md 路径，默认 <项目根>/skills/ascend-c/SKILL.md
# CANNEX_SKILL_PATH=
